CONVEX_DEPLOYMENT=local:local-leodev-amend
CONVEX_URL=http://127.0.0.1:3210
CONVEX_SITE_URL=http://127.0.0.1:3211

# Set with `bunx convex env set ...` for the active Convex deployment.
SITE_URL=http://amend.localhost:1355
BETTER_AUTH_SECRET=replace-with-a-generated-secret
GITHUB_WEBHOOK_SECRET=replace-with-your-github-webhook-secret
GITHUB_APP_ID=replace-with-your-github-app-id
GITHUB_APP_SLUG=replace-with-your-github-app-slug
GITHUB_APP_CLIENT_ID=replace-with-your-github-app-client-id
GITHUB_APP_CLIENT_SECRET=replace-with-your-github-app-client-secret
GITHUB_APP_PRIVATE_KEY=replace-with-your-github-app-private-key
AMEND_API_TOKEN=replace-with-your-api-token
# Discord application "Public Key" (hex) — used to verify the /ingest/discordInteraction endpoint.
DISCORD_PUBLIC_KEY=replace-with-your-discord-app-public-key
# Discord application (client) id — used to register the global /amend slash command.
DISCORD_APPLICATION_ID=replace-with-your-discord-application-id
# Discord bot token — registers slash commands and posts outbound channel messages.
DISCORD_BOT_TOKEN=replace-with-your-discord-bot-token
# Dev-only: allow signals with no channelRoutes match to fall back to
# DISCORD_DEFAULT_WORKSPACE_SLUG. Leave unset in production — unrouted signals
# are then rejected with 404 instead of landing in a single default workspace.
AMEND_DEV_DEFAULT_WORKSPACE_ALLOWED=1
# Workspace slug unrouted signals fall back to, ONLY when the dev flag above is
# set. Normal routing is per-channel via the channelRoutes table (claimed from
# Connections), not this env var.
DISCORD_DEFAULT_WORKSPACE_SLUG=replace-with-your-workspace-slug
POSTHOG_API_KEY=replace-with-your-posthog-project-key
POSTHOG_HOST=https://us.i.posthog.com
AMEND_AI_PROVIDER=openai
OPENAI_API_KEY=replace-with-your-provider-key
OPENAI_MODEL=gpt-5.1-mini
OPENROUTER_API_KEY=replace-with-your-openrouter-key
OPENROUTER_MODEL=nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free
CROF_API_KEY=replace-with-your-crof-key
CROF_MODEL=kimi-k2.6
CROF_BASE_URL=https://crof.ai/v1
# Optional: cheaper/faster model used only for signal triage (falls back to CROF_MODEL).
CROF_CLASSIFIER_MODEL=deepseek-v4-flash
RESEND_API_KEY=replace-with-your-email-provider-key
EMAIL_FROM=updates@example.com
STRIPE_SECRET_KEY=replace-with-your-billing-secret
STRIPE_WEBHOOK_SECRET=replace-with-your-stripe-webhook-secret
# PostHog personal API key — server-side analytics reads (convex/posthog.ts).
POSTHOG_PERSONAL_API_KEY=replace-with-your-posthog-personal-key
# executor.sh API key — outbound tool executions (POST /executions).
EXECUTOR_API_TOKEN=replace-with-your-executor-key

# --- Preview/CI-only (set by workflows; only needed for local preview debugging) ---
# AMEND_AUTH_ALLOWED_EMAILS=you@example.com,teammate@example.com
# AMEND_PREVIEW_AUTH_ENABLED=true
# AMEND_PREVIEW_SEEDING_ENABLED=true

# --- Optional build/version metadata (defaults to 0.0.0-dev when unset) ---
# AMEND_VERSION=
# AMEND_BUILD_SHA=
# AMEND_BUILD_TIME=
# AMEND_COMMIT_SHA=
# AMEND_DISABLE_VERSION_CHECK=
# AMEND_UPDATE_CHECK_URL=
